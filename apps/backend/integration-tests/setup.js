// Jest setup stub. Required by jest.config.js → setupFiles. Integration
// tests that boot a Medusa container do their own per-suite bootstrapping;
// this file exists so Jest can resolve the setupFiles path without error.
